package com.backend.curi.launchedmodule.repository;

import com.backend.curi.launchedmodule.repository.entity.LaunchedModule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LaunchedModuleRepository extends JpaRepository<LaunchedModule, Long> {
}
