package com.backend.curi.launched.repository;

import com.backend.curi.launched.repository.entity.LaunchedWorkflowManager;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LaunchedWorkflowManagerRepository extends JpaRepository<LaunchedWorkflowManager, Long> {
}
