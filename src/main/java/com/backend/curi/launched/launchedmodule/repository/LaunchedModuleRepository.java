package com.backend.curi.launched.launchedmodule.repository;

import com.backend.curi.launched.launchedmodule.repository.entity.LaunchedModule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LaunchedModuleRepository extends JpaRepository<LaunchedModule, Long> {
}
